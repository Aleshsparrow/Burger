<li>
	{{burger_name}}

	<button class="eat-burger" data-id="{{this.id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
